folder: ...  # insert the path of the folder where the profiler is
general:
  seed: 80
final_evaluation:
  n_holdout_clients: 100
  dataset_size: 20
  p: 0.9
hydra:
  output_subdir: ${folder}/.visualization
